<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Update Book</title>
    <link rel="stylesheet" type="text/css" href="updateBook.css">
</head>
<body>
    <h1>Update Book</h1>
    <form action="/book/<%= book._id %>" method="POST"> <!-- Adjust the form action and method as needed -->
        <input type="hidden" name="bookId" value="<%= book._id %>"> <!-- Hidden input to send book ID -->

        <div class="form-group">
            <label for="title">Title:</label>
            <input type="text" id="title" name="title" value="<%= book.title %>" required>
        </div>
        <div class="form-group">
            <label for="author">Author:</label>
            <input type="text" id="author" name="author" value="<%= book.author %>" required>
        </div>
        <div class="form-group">
            <label for="genre">Genre:</label>
            <input type="text" id="genre" name="genre" value="<%= book.genre %>">
        </div>
        <div class="form-group">
            <label for="publicationYear">Publication Year:</label>
            <input type="text" id="publicationYear" name="publicationYear" value="<%= book.publicationYear %>" required>
        </div>
        <div class="form-group">
            <label for="coverImage">Cover Image URL:</label>
            <input type="text" id="coverImage" name="coverImage" value="<%= book.coverImage %>">
        </div>
        <div class="form-group">
            <button type="submit">Update Book</button>
        </div>
    </form>
</body>
</html>
